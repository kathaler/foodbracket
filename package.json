{
  "name": "foodbracket",
  "version": "1.0.0",
  "description": "A web app for users to choose a local restaurant through an interactive, bracket-style tournament",
  "main": "index.js",
  "scripts": {
   "build:frontend": "pnpm --filter lit-frontend run build",
   "build:backend": "pnpm --filter express-backend run build",
   "build": "pnpm run build:frontend && pnpm run build:backend",
   "start": "pnpm run build && pnpm run start:backend",
   "start:frontend": "pnpm --filter lit-frontend run dev",
   "start:backend": "pnpm --filter express-backend run start"
 },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/kathaler/foodbracket.git"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.1.1"
  },
  "devDependencies": {
   "typescript": "^5.0.0"
  }
}
